apiVersion: apps/v1
kind: Deployment
metadata:
  name: lottery-orchestrator
  namespace: lottery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lottery-orchestrator
  template:
    metadata:
      labels:
        app: lottery-orchestrator
    spec:
      containers:
      - name: lottery-orchestrator
        image: quay.io/jeremyh/lottery-orchestrator:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: postgresql+asyncpg://postgres:postgres@postgres:5432/orchestrator
        - name: REDIS_URL
          value: redis://redis:6379
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          value: http://splunk-otel-collector:4317
        - name: OTEL_SERVICE_NAME
          value: lottery-orchestrator
        - name: DEPLOYMENT_ENV
          value: production
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: lottery-orchestrator
  namespace: lottery
spec:
  selector:
    app: lottery-orchestrator
  ports:
  - port: 8000
    targetPort: 8000

